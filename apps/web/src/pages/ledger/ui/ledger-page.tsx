export function LedgerPage() {
  return (
    <section>
      <h1>Cycle Ledger</h1>
      <p>
        Every entry in one cycle, in due-date order, with the balance after
        each.
      </p>
    </section>
  );
}
